version: "2.1"
services:
  freshrss:
    image: linuxserver/freshrss
    container_name: freshrss
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Helsinki
    volumes:
      - "/docker/freshrss:/config"
    ports:
      - 8088:80
    restart: unless-stopped
